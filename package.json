{
  "name": "digital-garden-starter",
  "version": "1.0.0",
  "description": "Template Repo for digital garden starter",
  "private": true,
  "scripts": {
    "develop": "gatsby develop",
    "rebuild": "gatsby clean && gatsby build",
    "build": "gatsby build --prefix-paths",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public"
  },
  "dependencies": {
    "gatsby": "^2.32.13",
    "gatsby-theme-garden": "https://github.com/ibnishak/digital-garden-theme.git",
    "gh-pages": "^3.1.0"
  }
}
